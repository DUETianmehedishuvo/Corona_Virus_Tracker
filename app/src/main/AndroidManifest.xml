<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mehedi.koronaviruscausesTusu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DonateActivity"></activity>
        <activity android:name=".SearchGlobalDayByDayActivity" />
        <activity android:name=".BangladeshDayByDayActivity" />
        <activity android:name=".Search_Spicify_Country_Activity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".AboutUs" />
        <activity android:name=".ShowDayByDay" />
        <activity android:name=".ShowAllCountry" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>